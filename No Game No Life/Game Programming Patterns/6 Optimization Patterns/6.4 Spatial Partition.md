_Efficiently locate objects by storing them in a data structure organized by their positions._

## 1 Motivation
游戏世界有非常多的对象，游戏引擎需要处理对象附近有什么物体的问题，比如我们需要知道音源与角色的相对位置等等。但如果我们在每帧遍历游戏场景中的每一个物体肯定不现实，$n^2$
>[[Lec3 How to Build a Game World]]中有具体的描述

所以我们可以对空间进行分区，这样就可以一次只对少个物体进行内部的判断，节省很多的运行时间。

**空间分区模式**：“对于一组对象而言，每一个对象在空间都有一个位置。将对象存储在一个根据对象的位置来组织的数据结构中，该数据结构可以让你高效地查询位于或靠近某处的对象。当对象的位置变化时，应更新该空间数据结构以便可以继续这样查找对象。”

这个模式带来的问题是：当物体进行位置改变时需要更多的处理。

---

## 2 Samples
我们就以最简单的 `fixed grid` 举例好了。我们的世界是一个巨大的`grid map`，内部的物体是一个个`unit`，我们用链表来存储这些`unit`
我们需要解决的问题是：
1. grid 和 unit 的相互作用
2. 如何处理unit的位置移动
3. 如何处理跨unit的交互

### 2.1 Grid & Unit
我们先来看看grid和unit的实现：
```cpp
class Unit
{
  friend class Grid;

public:
  Unit(Grid* grid, double x, double y)
  : grid_(grid),
    x_(x),
    y_(y),
    prev_(NULL),
    next_(NULL)
  {
    grid_->add(this)
  }

  void move(double x, double y);

private:
  double x_, y_;
  Grid* grid_;
  
  // 链表
  Unit* prev_;
  Unit* next_;
};
```

```cpp
class Grid
{
public:
  Grid()
  {
    // Clear the grid.
    for (int x = 0; x < NUM_CELLS; x++)
    {
      for (int y = 0; y < NUM_CELLS; y++)
      {
        cells_[x][y] = NULL;
      }
    }
  }

  static const int NUM_CELLS = 10;
  static const int CELL_SIZE = 20;
private:
  Unit* cells_[NUM_CELLS][NUM_CELLS];
};
```

我们现在需要实现向Grid中加入Unit，以及遍历Unit
```cpp
void Grid::add(Unit* unit)
{
  // Determine which grid cell it's in.
  int cellX = (int)(unit->x_ / Grid::CELL_SIZE);
  int cellY = (int)(unit->y_ / Grid::CELL_SIZE);

  // Add to the front of list for the cell it's in.
  unit->prev_ = NULL;
  unit->next_ = cells_[cellX][cellY];
  cells_[cellX][cellY] = unit;

  if (unit->next_ != NULL)
  {
    unit->next_->prev_ = unit;
  }
}
```
>这是一个很传统的双指针链表的头插入

我们现在就可以遍历每一个grid中的cell，然后调用`handleCell`方法：
```cpp
void Grid::handleCell(Unit* unit)
{
  while (unit != NULL)
  {
    Unit* other = unit->next_;
    while (other != NULL)
    {
      if (unit->x_ == other->x_ &&
          unit->y_ == other->y_)
      {
        handleAttack(unit, other);
      }
      other = other->next_;
    }

    unit = unit->next_;
  }
}
```

### 2.2 Unit跨cell移动
我们为Unit创建移动方法：
```cpp
void Unit::move(double x, double y)
{
  grid_->move(this, x, y);
}
```

```cpp
void Grid::move(Unit* unit, double x, double y)
{
  // See which cell it was in.
  int oldCellX = (int)(unit->x_ / Grid::CELL_SIZE);
  int oldCellY = (int)(unit->y_ / Grid::CELL_SIZE);

  // See which cell it's moving to.
  int cellX = (int)(x / Grid::CELL_SIZE);
  int cellY = (int)(y / Grid::CELL_SIZE);

  unit->x_ = x;
  unit->y_ = y;

  // If it didn't change cells, we're done.
  if (oldCellX == cellX && oldCellY == cellY) return;

  // Unlink it from the list of its old cell.
  if (unit->prev_ != NULL)
  {
    unit->prev_->next_ = unit->next_;
  }

  if (unit->next_ != NULL)
  {
    unit->next_->prev_ = unit->prev_;
  }

  // If it's the head of a list, remove it.
  if (cells_[oldCellX][oldCellY] == unit)
  {
    cells_[oldCellX][oldCellY] = unit->next_;
  }

  // Add it back to the grid at its new cell.
  add(unit);
}
```
>其实就是一个在原链表去除，然后再add到新链表

### 2.3 跨Cell判断
还有一个情况是两个cell中临近的物体如何处理。我们可以用穷举的方法。
```cpp
void Grid::handleCell(int x, int y)
{
  Unit* unit = cells_[x][y];
  while (unit != NULL)
  {
    // Handle other units in this cell.
    handleUnit(unit, unit->next_);

    // Also try the neighboring cells.
    if (x > 0 && y > 0) handleUnit(unit, cells_[x - 1][y - 1]);
    if (x > 0) handleUnit(unit, cells_[x - 1][y]);
    if (y > 0) handleUnit(unit, cells_[x][y - 1]);
    if (x > 0 && y < NUM_CELLS - 1)
    {
      handleUnit(unit, cells_[x - 1][y + 1]);
    }

    unit = unit->next_;
  }
}
```
