---
title: "归并排序"
category: 算法手撕高频题
tags: [算法, 手撕代码, 归并排序, 排序, 网易互娱]
frequency: ⭐
difficulty: 中等
companies: [网易互娱]
status: new
last_reviewed: 
next_review: 
related:
  - "[[快速排序]]"
  - "[[堆排序]]"
  - "[[排序算法]]"
---

## 🎯 一句话结论（自测用）
> 分治：找中点 -> 递归排序左右 -> 合并两个有序数组。O(nlogn) 稳定，空间 O(n)。适合链表排序和外部排序。

## ✅ 标准答案（结构化、可背诵，2分钟内讲完）
1. 分治思想：将数组分成两半，分别排序，再合并
2. 找中点，递归调用 mergeSort
3. 合并（merge）：双指针比较两个有序子数组，较小的放入临时数组
4. 时间复杂度：O(nlogn)，稳定；空间复杂度：O(n)
5. 相比快排：稳定、无退化风险，但需要 O(n) 额外空间

## 🔍 详细解析

### C++ 标准实现
```cpp
// 合并两个有序子数组
void merge(vector<int>& nums, int left, int mid, int right) {
    vector<int> temp(right - left + 1);
    int i = left, j = mid + 1, k = 0;

    // 双指针比较，小的先放
    while (i <= mid && j <= right) {
        if (nums[i] <= nums[j]) temp[k++] = nums[i++];
        else temp[k++] = nums[j++];
    }
    // 剩余元素
    while (i <= mid) temp[k++] = nums[i++];
    while (j <= right) temp[k++] = nums[j++];
    // 拷贝回原数组
    for (int p = 0; p < k; p++) nums[left + p] = temp[p];
}

void mergeSort(vector<int>& nums, int left, int right) {
    if (left >= right) return;
    int mid = left + (right - left) / 2;
    mergeSort(nums, left, mid);
    mergeSort(nums, mid + 1, right);
    merge(nums, left, mid, right);
}
```

### Csharp 版本
```csharp
public static class MergeSort
{
    public static void Sort(int[] arr, int left, int right)
    {
        if (left >= right) return;
        int mid = left + (right - left) / 2;
        Sort(arr, left, mid);
        Sort(arr, mid + 1, right);
        Merge(arr, left, mid, right);
    }

    private static void Merge(int[] arr, int left, int mid, int right)
    {
        int[] temp = new int[right - left + 1];
        int i = left, j = mid + 1, k = 0;

        while (i <= mid && j <= right)
            temp[k++] = arr[i] <= arr[j] ? arr[i++] : arr[j++];
        while (i <= mid) temp[k++] = arr[i++];
        while (j <= right) temp[k++] = arr[j++];

        Array.Copy(temp, 0, arr, left, temp.Length);
    }
}
```

### 链表归并排序（重点）
面试可能问链表的归并排序，要求 O(1) 额外空间：
```cpp
ListNode* mergeTwoLists(ListNode* l1, ListNode* l2) {
    ListNode dummy(0), *tail = &dummy;
    while (l1 && l2) {
        if (l1->val <= l2->val) { tail->next = l1; l1 = l1->next; }
        else { tail->next = l2; l2 = l2->next; }
        tail = tail->next;
    }
    tail->next = l1 ? l1 : l2;
    return dummy.next;
}

ListNode* sortList(ListNode* head) {
    if (!head || !head->next) return head;
    // 快慢指针找中点
    ListNode *slow = head, *fast = head->next;
    while (fast && fast->next) {
        slow = slow->next;
        fast = fast->next->next;
    }
    ListNode* mid = slow->next;
    slow->next = nullptr;
    ListNode* left = sortList(head);
    ListNode* right = sortList(mid);
    return mergeTwoLists(left, right);
}
```

### 归并排序 vs 快速排序
| | 归并排序 | 快速排序 |
|--|---------|---------|
| 稳定性 | 稳定 | 不稳定 |
| 空间复杂度 | O(n) | O(log n) |
| 最坏时间复杂度 | O(nlogn) | O(n^2) |
| 链表排序 | 适合 | 不适合（需要随机访问） |
| 外部排序 | 适合 | 不适合 |

## 💬 面试官常见追问
- "归并排序的空间复杂度能否优化？" -> 原地归并（in-place merge）可以降到 O(1)，但实现复杂且时间常数变大
- "什么时候用归并不用快排？" -> 需要稳定性、链表排序、外部排序
- "归并排序如何用于外部排序？" -> 多路归并：分块读入内存排序 -> 写回磁盘 -> 多路合并

## ⚠️ 我曾经的误区 / 网上常见错答
- **误区**：merge 中 while 条件写成 i <= mid && j <= right。正确，注意 mid 和 right 的边界
- **误区**：合并后忘记拷贝回原数组。temp 是局部变量，不拷贝回去原数组不变

## 🔗 关联知识点
- [[快速排序]]
- [[堆排序]]
- [[排序算法]]
- [[链表高频操作]]

## 📎 原始出处
- 012_互娱 Q9: 快排/归并排序时间复杂度、退化情况
- GitHub 一: 归并排序稳定 O(nlogn)
