TCP和UDP的问题都显而易见。

UDP可以同过自己造轮子来实现可考传输：
- 发送时：为每个数据包打上一个唯一标识
- 接收端：检查数据包，并会送确认消息
- 发送端：处理确认消息，管理超时和重发

而从头开始造轮子比较困难，所以会使用E-Net。
- 保序：给每个包打上一个递增的序列号
- Channels：可以使不同的业务、不同可靠性要求数据放到不同频道（即使放在不可靠传输，也会保序传，不会乱序）
- 数据包的分片和重组。发送时分成小块，接收时拼接成完整的数据包

